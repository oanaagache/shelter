<Navbar className="navbar navbar-dark bg-primary">
        <Navbar.Brand href="/">Event.ro</Navbar.Brand>
        <Navbar.Toggle aria-controls="basic-navbar-nav" />
        <Navbar.Collapse id="basic-navbar-nav">
          <Nav className="mr-auto">
            <NavLink
              to="/"
              // className={({ isActive }) => (isActive ? "active" : "inactive")}
            >
              Events
            </NavLink>
            <NavLink
              to="/FormEvent"
              // className={({ isActive }) => (isActive ? "active" : "inactive")}
            >
              Form
            </NavLink>

            <NavLink
              to="/basket"
              // className={({ isActive }) => (isActive ? "active" : "inactive")}
            >
              Basket
            </NavLink>
            <NavLink
              to="/main"
              // className={({ isActive }) => (isActive ? "active" : "inactive")}
            >
              Main
            </NavLink>
            <NavLink
              to="/ticket"
              // className={({ isActive }) => (isActive ? "active" : "inactive")}
            >
              ticket
            </NavLink>
          </Nav>
        </Navbar.Collapse>
      </Navbar>